all python code lives here

# Offline RAG Pipeline

## Requirements
- Python 3.12 (3.14 not supported due to ChromaDB/Pydantic compatibility)
- Ollama running locally
```

You could also add a `.python-version` file in the project root:
```
3.12